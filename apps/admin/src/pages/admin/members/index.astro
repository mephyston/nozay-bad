---
import Layout from '../../../layouts/Layout.astro';
import AdminLayout from '../../../components/AdminLayout.svelte';
import MembersScreen from '../../../components/MembersScreen.svelte';

const parametres = Object.fromEntries(Astro.url.searchParams);
---

<Layout title="Gestion des Adhérents - NBA 91">
  <AdminLayout
    email={Astro.locals.user?.email || ''}
    realEmail={Astro.locals.realUser?.email || ''}
    name={Astro.locals.user?.name}
    client:idle
    breadcrumb="Adhérents / Liste"
    permissions={Astro.locals.user?.permissions || []}
  >
    <div class="space-y-6">
      <MembersScreen client:load {parametres} />
    </div>
  </AdminLayout>
</Layout>
