---
/*
  L'ancienne adresse de la Caisse. L'écran est devenu celui de tout compte sans relevé,
  servi à `/admin/accounting/accounts/<code>` ; la saison choisie suit dans l'URL.
*/
return Astro.redirect(`/admin/accounting/accounts/cash${Astro.url.search}`, 301);
---
