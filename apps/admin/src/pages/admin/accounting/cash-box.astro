---
/*
  L'ancienne adresse de la Caisse. L'écran est devenu celui de tout compte sans relevé,
  servi à `/admin/accounting/accounts/<code>` ; la caisse n'a plus de code réservé, le
  menu mène à chacune. On renvoie donc au tableau de bord de la comptabilité.
*/
return Astro.redirect(`/admin/accounting${Astro.url.search}`, 301);
---
