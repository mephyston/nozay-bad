---
import Layout from '../../../layouts/Layout.astro';
import AdminLayout from '../../../components/AdminLayout.svelte';
import CashBoxScreen from '../../../components/CashBoxScreen.svelte';

const parametres = Object.fromEntries(Astro.url.searchParams);
---

<Layout title="Caisse - NBA 91">
  <AdminLayout
    realEmail={Astro.locals.realUser?.email || ''}
    name={Astro.locals.user?.name}
    client:idle
    email={Astro.locals.user?.email || ''}
    permissions={Astro.locals.user?.permissions || []}
    breadcrumb="Comptabilité / Caisse"
  >
    <div class="space-y-6">
      <CashBoxScreen client:load {parametres} />
    </div>
  </AdminLayout>
</Layout>
