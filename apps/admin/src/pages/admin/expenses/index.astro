---
import Layout from '../../../layouts/Layout.astro';
import AdminLayout from '../../../components/AdminLayout.svelte';
import ExpensesScreen from '../../../components/ExpensesScreen.svelte';

const parametres = Object.fromEntries(Astro.url.searchParams);
const initialAction = Astro.url.searchParams.get('action');
---

<Layout title="Notes de Frais - NBA 91">
  <AdminLayout
    realEmail={Astro.locals.realUser?.email || ''}
    name={Astro.locals.user?.name}
    client:idle
    email={Astro.locals.user?.email || ''}
    permissions={Astro.locals.user?.permissions || []}
    breadcrumb="Comptabilité / Notes de frais"
  >
    <div class="space-y-6">
      <ExpensesScreen client:load {parametres} {initialAction} />
    </div>
  </AdminLayout>
</Layout>
