---
import Layout from '../../../layouts/Layout.astro';
import AdminLayout from '../../../components/AdminLayout.svelte';
import DayValuesScreen from '../../../components/DayValuesScreen.svelte';

/*
  Page figée : elle n'est plus rendue par un worker, mais servie depuis les actifs.
  Elle ne porte donc aucune donnée, aucune identité et aucun paramètre d'URL — l'îlot
  demande les siennes au relais, et lit l'URL dans le navigateur.
*/
export const prerender = true;
---

<Layout title="Contrôle des journées - NBA 91">
  <AdminLayout
    client:idle
    breadcrumb="Contrôle des journées"
  >
    <div class="space-y-6">
      <DayValuesScreen client:load />
    </div>
  </AdminLayout>
</Layout>
