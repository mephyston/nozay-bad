{
  "name": "website",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/website/src",
  "targets": {
    "lint": {
      "command": "eslint {projectRoot}"
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "inputs": [
        "production",
        "^production",
        {
          "env": "PUBLIC_APP_ENV"
        }
      ],
      "options": {
        "command": "astro build",
        "cwd": "apps/website"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "command": "astro dev --port 4323",
        "cwd": "apps/website"
      }
    }
  },
  "tags": [
    "type:app",
    "scope:website"
  ]
}
