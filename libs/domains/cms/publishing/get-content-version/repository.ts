/**
 * Pas de dépôt propre à cette tranche.
 *
 * La lecture de la version tient en une ligne et sert à trois capacités (pages,
 * actualités, menus) : elle vit dans `shared/cache-version.ts` plutôt que d'être
 * recopiée dans chacune. Ce fichier existe pour que la tranche reste lisible au même
 * gabarit que les autres.
 */
export {};
