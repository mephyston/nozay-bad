import { createInternalTransferSchema } from '../create-internal-transfer/validator';

/** Le même corps qu'à la création : voir `dto.ts`. */
export const updateInternalTransferSchema = createInternalTransferSchema;
